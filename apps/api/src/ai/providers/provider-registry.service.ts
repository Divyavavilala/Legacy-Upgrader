/**
 * Alias for {@link AiProviderRegistry} per platform naming conventions.
 * Prefer importing `AiProviderRegistry` in new code.
 */
export { AiProviderRegistry as ProviderRegistryService } from './ai-provider.registry';
